NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Only needed for the "invite a second admin" feature — never commit the real value,
# and never expose it to the browser (no NEXT_PUBLIC_ prefix). Add it as an
# environment variable in the Vercel dashboard, not in this file.
# SUPABASE_SERVICE_ROLE_KEY=
